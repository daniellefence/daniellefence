<div class="relative isolate pt-14 bg-gradient-to-r from-brand-light to-white">
    <div class="mx-auto max-w-7xl px-6 lg:px-8 py-16">
        <div class="mx-auto text-center">
            <h1 class="text-4xl font-bold tracking-tight text-brand-primary sm:text-6xl font-display">{{$heading}}</h1>
            @if(isset($subheading))
                <p class="mt-6 text-lg text-brand-neutral/80 max-w-3xl mx-auto">{{$subheading}}</p>
            @endif
        </div>
    </div>
</div>
